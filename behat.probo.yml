imports:
    - behat.common.yml

probo:
  extensions:
    Behat\MinkExtension:
      base_url: http://localhost
    Drupal\DrupalExtension:
      api_driver: 'drupal'
      drush:
        alias: 'local'
      drupal:
        drupal_root: '/var/www/html'